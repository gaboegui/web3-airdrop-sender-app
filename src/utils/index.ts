/**
 * enable to use the imports directly in this format:
 *      import { calculateTotalAmount } from "@/utils";
 */
export {calculateTotalAmount, calculateTotal} from "./calculate/calculateTotalAmount"